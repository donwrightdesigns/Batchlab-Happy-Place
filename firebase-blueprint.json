{
  "entities": {
    "User": {
      "title": "User",
      "description": "User profile information",
      "type": "object",
      "properties": {
        "uid": { "type": "string" },
        "email": { "type": "string", "format": "email" },
        "displayName": { "type": "string" },
        "photoURL": { "type": "string" },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["uid", "email"]
    },
    "Enhancement": {
      "title": "Enhancement",
      "description": "A processed photo enhancement record",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "timestamp": { "type": "number" },
        "prompt": { "type": "string" },
        "originalImage": { "type": "string", "description": "Base64 string of original image" },
        "editedImage": { "type": "string", "description": "Base64 string of processed image" },
        "settings": {
          "type": "object",
          "properties": {
            "model": { "type": "string" },
            "resolution": { "type": "string" }
          }
        }
      },
      "required": ["id", "timestamp", "prompt", "originalImage", "editedImage"]
    },
    "Batch": {
      "title": "Batch",
      "description": "A collection of processed images",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "timestamp": { "type": "number" },
        "title": { "type": "string" },
        "address": { "type": "string" },
        "imageIds": { "type": "array", "items": { "type": "string" } },
        "thumbnails": { "type": "array", "items": { "type": "string" } },
        "tags": { "type": "array", "items": { "type": "string" } },
        "isSynced": { "type": "boolean" }
      },
      "required": ["id", "timestamp", "title", "imageIds", "thumbnails"]
    },
    "FavoritePrompt": {
      "title": "FavoritePrompt",
      "description": "A saved user prompt for repeated use",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "name": { "type": "string" },
        "prompt": { "type": "string" },
        "timestamp": { "type": "number" }
      },
      "required": ["id", "name", "prompt", "timestamp"]
    },
    "Settings": {
      "title": "Settings",
      "description": "User specific app settings",
      "type": "object",
      "properties": {
        "instruction": { "type": "string" }
      },
      "required": ["instruction"]
    }
  },
  "firestore": {
    "/users/{userId}": {
      "schema": "User",
      "description": "Private user profile"
    },
    "/users/{userId}/enhancements/{enhancementId}": {
      "schema": "Enhancement",
      "description": "User's private enhancement history"
    },
    "/users/{userId}/batches/{batchId}": {
      "schema": "Batch",
      "description": "User's private photo batches/collections"
    },
    "/users/{userId}/favorites/{favId}": {
      "schema": "FavoritePrompt",
      "description": "User's saved favorite prompts"
    },
    "/users/{userId}/settings/{settingId}": {
      "schema": "Settings",
      "description": "User's saved app settings"
    }
  }
}
